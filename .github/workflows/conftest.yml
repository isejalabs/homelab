name: "Conftest"
on:
  pull_request:
  push:
    branches:
    - main

jobs:
  conftest:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
    - run: scripts/install-conftest.sh "$HOME/.local/bin"
    - run: echo "$HOME/.local/bin" >> "$GITHUB_PATH"
    - run: scripts/conftest-test-all.sh
