apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: adguard

resources:
  - ns.yaml
  - svc.yaml
  - svc-ui.yaml
  - secret-users.yaml
  - deployment.yaml
  - http-route.yaml
configMapGenerator:
  - name: adguard-config
    files:
      - config/AdGuardHome.yaml

