---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: powerdns
spec:
  values:
    controllers:
      main:
        containers:
          main:
            env:
              ZONE: src.iseja.net
        initContainers:
          bootstrap-zone:
            env:
              ZONE: src.iseja.net
