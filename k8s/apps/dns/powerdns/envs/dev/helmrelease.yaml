---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: powerdns
spec:
  values:
    controllers:
      main:
        containers:
          bootstrap-zone:
            env:
              ZONE: dev.iseja.net
          main:
            env:
              ZONE: dev.iseja.net
