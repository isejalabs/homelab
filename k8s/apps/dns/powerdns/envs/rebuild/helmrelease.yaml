---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: powerdns
spec:
  values:
    controllers:
      main:
        containers:
          bootstrap-zone:
            env:
              ZONE: rebuild.iseja.net
          main:
            env:
              ZONE: rebuild.iseja.net
