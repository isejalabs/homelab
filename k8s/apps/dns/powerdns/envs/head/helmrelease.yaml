---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: powerdns
spec:
  values:
    controllers:
      main:
        containers:
          bootstrap-zone:
            env:
              ZONE: head.iseja.net
          main:
            env:
              ZONE: head.iseja.net
