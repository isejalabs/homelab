---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: powerdns
spec:
  values:
    controllers:
      main:
        containers:
          main:
            env:
              ZONE: poc.iseja.net
        initContainers:
          bootstrap-zone:
            env:
              ZONE: poc.iseja.net
