---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: powerdns

components:
  - ../../../../components/apps/storage/pvc

resources:
  - ns.yaml
  - externalsecret.yaml
  - helmrelease.yaml
  - ocirepository.yaml
