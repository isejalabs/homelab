apiVersion: apps/v1
kind: Deployment
metadata:
  name: kopiur-test
  labels:
    app: kopiur-test
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: kopiur-test
  template:
    metadata:
      namespace: kopiur-test
      labels:
        app: kopiur-test
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: kopiur-test
          image: docker.io/library/busybox:1.37@sha256:9db7b59979c38555a39def84a31fb98b5296952f9e3afd4f6f11f05b07adfab0
          command: ["/bin/sh", "-c"]
          args:
            - |
              echo "hello from $(hostname) at $(date -Iseconds)" >> /data/marker.txt
              cat /data/marker.txt
              sleep infinity
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: 10m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 32Mi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: kopiur-test
