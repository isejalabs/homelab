apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: actualbudget
spec:
  hostnames:
    - budget.example.com
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: internal
      namespace: gateway-api
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: actualbudget
          port: 5006
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
