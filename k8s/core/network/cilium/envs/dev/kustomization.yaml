apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ip-pool-bgp.yaml
  - bgp-cluster-config.yaml

# Example for local environment specific values
# configMapGenerator:
#   - name: env-values
#     files:
#       - values.yaml
# 
# generatorOptions:
#   disableNameSuffixHash: true
# 
# patches:
#   - target:
#       kind: HelmRelease
#       name: cilium
#     patch: |-
#       - op: replace
#         path: /spec/valuesFrom/0/name
#         value: env-values
