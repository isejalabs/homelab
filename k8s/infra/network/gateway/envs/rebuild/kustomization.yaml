apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

components:
  - ../../../../../_components/rebuild

resources:
  - ../../base

patches:
  - path: cert.yaml
  - path: gw-external.yaml
  - path: gw-internal.yaml
