apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: cloudflare-cluster-issuer
spec:
  acme:
    email: prod-certmanager@iseja.net
    server: https://acme-v02.api.letsencrypt.org./directory