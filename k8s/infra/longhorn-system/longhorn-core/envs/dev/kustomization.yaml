apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

components:
  - ../../../../../components/envs/dev

resources:
  - ../../base

patches:
  - path: backup-target.yaml
  - path: minio-backup-secret.yaml
