apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

components:
  - ../../../../../components/envs/qa

resources:
  - ../../base

patches:
  - path: backup-target.yaml
  - path: minio-backup-secret.yaml
