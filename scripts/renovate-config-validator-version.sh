#!/bin/sh

# Sourced by CI to pin the `renovate` npm package version whose bundled
# `renovate-config-validator` binary validates .github/renovate*.
# shellcheck disable=SC2034
RENOVATE_VERSION="44.107.3" # renovate: npm=renovate
